% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mt_anno_pathways_HMDB.R
\name{mt_anno_pathways_HMDB}
\alias{mt_anno_pathways_HMDB}
\title{Add pathway information.}
\usage{
mt_anno_pathways_HMDB(
  D,
  in_col,
  out_col,
  pwdb_name = "SMP",
  db_dir = data.makepath("MT_precalc/hmdb/"),
  db_filename,
  export_raw_db
)
}
\arguments{
\item{D}{\code{SummarizedExperiment} input}

\item{in_col}{Column to use for pathway fetching. The selected column must contain metabolite identifiers (e.g. HMBD, KEGG, ChEBI, etc)}

\item{out_col}{A new column name for D to output pathway information to}

\item{pwdb_name}{Name of the pathway database to use. Can use either SMP or KEGG for this argument}

\item{db_dir}{Name of the directory where the parsed HMDB files are stored}

\item{db_filename}{Filename of the parsed HMDB file to use. Must be in the format: hmdb_preprocessed_{version_number}.rds}

\item{export_raw_db}{OPTIONAL. Export the pathway database to a directory. Must be a string containing the path name with a .xlsx extension.}
}
\value{
column out_col in SE containing pathway annotation for metabolites

$pathways: a dataframe of pathway information
}
\description{
Adds custom pathways to the already existing SummarizedExperiment
data structure using the HMDB .xml file.
}
\examples{
# annotate metabolites using smp_db
\dontrun{... \%>\%
  mt_anno_pathways_HMDB(in_col = "HMDb_ID", out_col = "smp_db",
                        pwdb_name = "SMP",
                        db_dir = codes.makepath("snippets/packages/metabotools_external/hmdb")) \%>\%
...}

}
\author{
Parviz Gomari
}
