% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mt_pre_filtermiss.R
\name{mt_pre_filtermiss}
\alias{mt_pre_filtermiss}
\title{Filter by missingness.}
\usage{
mt_pre_filtermiss(
  D,
  met_max = NA,
  sample_max = NA,
  met_group = NA,
  log_filtered = F,
  log_sample_col = ""
)
}
\arguments{
\item{D}{\code{SummarizedExperiment} input}

\item{met_max}{Maximum fraction of missing metabolites (between 0 and 1.0)}

\item{sample_max}{Maximum fraction of missing samples (between 0 and 1.0)}

\item{met_group}{Name of of a colData sample annotation column; met_max will be applied to each group separately, metabolite must have at most met_max in any of the groups.}

\item{log_filtered}{Write list of filtered metabolites into the log text? Default: False}

\item{log_sample_col}{Required if log_filtered=T for sample filtering. Specifies which column of colData() to output into the log.}
}
\value{
SE rows or columns will be filtered.

$output: logical vector of kept metabolites/samples
}
\description{
Filters either samples or metabolites, won't do both in one call.
}
\examples{
\dontrun{# first remove samples with >10\% missingness, then metabolites with >20\% missingness
... \%>\%
  mt_pre_filtermiss(sample_max=0.1) \%>\%
  mt_pre_filtermiss(met_max=0.2) \%>\%
...}

}
\author{
JK
}
