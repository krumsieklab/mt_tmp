% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mt_plots_boxplot.R
\name{mt_plots_boxplot}
\alias{mt_plots_boxplot}
\title{Boxplots}
\usage{
mt_plots_boxplot(
  D,
  x = "x",
  statname,
  correct_confounder,
  metab_filter = p.value < 0.05,
  metab_sort = p.value,
  annotation = "{sprintf('P-value: \%.1e', p.value)}",
  text.size = 3.88,
  jitter = T,
  rows,
  cols,
  restrict.to.used.samples = T,
  full.info = F,
  manual.ylab = NULL,
  ggadd = NULL,
  ...
)
}
\arguments{
\item{D}{\code{SummarizedExperiment} input}

\item{x}{what phenotype (from colData(D)) should be used on x axis, default "x"}

\item{statname}{index of the entry in metadata(D)$results that contains statistic object}

\item{correct_confounder}{confounders to adjust for before plotting, formula notation}

\item{metab_filter}{if given, filter will be applied to data and remaining variables will be labelled in plot, default p.value<0.05}

\item{metab_sort}{if given, arrange will be applied to data variables will be sorted, default p.value}

\item{text.size}{text size of the annotations}

\item{jitter}{whether to add jitter to boxplot,  default T}

\item{rows}{number rows of boxplots in $result}

\item{cols}{number columns of boxplots in $result}

\item{restrict.to.used.samples}{whether to filter to the samples that were used in the statistical test, default: T}

\item{full.info}{add full information of all sample annotations and statistics results to plottable data.frame? makes plotting more flexible but can render SE objects huge. default: F}

\item{manual.ylab}{manual ylabel (default: none)}

\item{ggadd}{further elements/functions to add (+) to the ggplot object}

\item{...}{additional expression directly passed to aes() of ggplot, can refer to colData}
}
\value{
$result: plot, boxplot
}
\description{
Creates one boxplot plot per metabolite based on given sample annotations
}
\examples{
\dontrun{# boxplots as overview of results with a result already in 'comp'
# color by "Group" variable in colData
mt_plots_boxplot(x                  = Group,
                 statname           = "comp",
                 correct_confounder = ~BATCH_MOCK,
                 metab_filter       = p.value<0.01,
                 metab_sort         = p.value,
                 annotation         = "{sprintf('P-value: \%.1e', p.value)}\nStatistic: {sprintf('\%.2f', statistic)}",
                 rows               = 2,
                 cols               = 2) \%>\%
                 ...}

 @author Jonas Zierer

 @import ggplot2
 @importFrom magrittr \%>\% \%<>\%
 @import SummarizedExperiment

 @export
}
